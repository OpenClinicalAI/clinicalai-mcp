node_modules/
dist/
*.tsbuildinfo
coverage/

# secrets — copy .env.example to .env and fill in any API keys you have
.env
.env.local
.env.*.local

# private notes — never publish
*.private.md
notes/

# per-user local config (Claude Code permission allow-list, machine-specific
# paths in claude-desktop-config.local.json, etc.) — by convention .local.json
# files stay on the contributor's machine.
*.local.json
.claude/

# local cache + sqlite artifacts
.clinicalai-mcp/
*.db
*.db-wal
*.db-shm

# editor / os
.DS_Store
*.log
.idea/
